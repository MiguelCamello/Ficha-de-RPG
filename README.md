# Ficha-de-RPG
Um sistema CRUD em python que simula um sistema no terminal que pode criar, buscar, listar e editar os dados de ficha rpg que ficam salvos em um json
Para rodar o projeto, de play dentro do main.py